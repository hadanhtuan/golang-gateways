syntax = "proto3";

package userService;

import "proto/sdk/sdk.proto";

option go_package = "user-gateway/proto/user";

service userService {
  rpc login(MessageLogin) returns (sdk.BaseResponse);
}

message MessageLogin {
  string username = 1;
  string password = 4;
}
